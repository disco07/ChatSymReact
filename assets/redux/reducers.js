
const initialState = {
    items: [],
    isLoading: false,
}

const conversation = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default conversation;
